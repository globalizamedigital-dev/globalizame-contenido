import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";import{extractPosts}from"../lib/resources.mjs";test("extrae el calendario canónico",()=>{const html=fs.readFileSync(new URL("../../recursos/estrategia_mes.html",import.meta.url),"utf8"),posts=extractPosts(html,2026);assert.ok(posts.length>=1);assert.ok(posts.every(p=>p.date&&p.title&&p.stage&&p.cta));const stages=new Set(posts.map(p=>p.stage));assert.ok(stages.has("TOFU")&&stages.has("BOFU"),"el calendario debe recorrer del TOFU al BOFU")});
