import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";import{extractPosts}from"../lib/resources.mjs";test("extrae el calendario canónico",()=>{const html=fs.readFileSync(new URL("../../recursos/estrategia_mes.html",import.meta.url),"utf8"),posts=extractPosts(html,2026);assert.ok(posts.length>=10);assert.ok(posts.every(p=>p.date&&p.title&&p.stage&&p.cta))});
